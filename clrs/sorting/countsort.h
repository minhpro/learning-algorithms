
/**
* Sort an positive array whose element are smaller max value
* Store the sorted in other array b
*/
void countsort(int a[], int b[], int len, int max);

/** 
* Sort an array whose element are in a range
* Store the sorted in other array b
*/
void rangeCountSort(int a[], int b[], int len, int min, int max);

